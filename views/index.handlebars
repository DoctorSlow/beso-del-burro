<h1>Burros!</h1>

<h2>Check out our tasty burros!</h2>
{{!-- left side --}}
<ul>
  {{#each burros}} {{#unless devoured}} {{> burros/burro-block devoured=true}} {{/unless}} {{/each}}
</ul>

<h2>Check out which burros you've already eaten!</h2>
{{!-- right side --}}
<ul>
  {{#each burros}} {{#if devoured}} {{> burros/burro-block devoured=false}} {{/if}} {{/each}}
</ul>

<h2>Add a Burro</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burro Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label>
    <br>
    <input type="radio" name="devoured" value="1" checked> Devoured!
    <br>
    <input type="radio" name="devoured" value="0"> Uneaten
  </div>

  <button type="submit">Add Burro</button>
</form>